# DistributedServerClientC
Basic example of distributed server &amp; client in C. To run, you must open two IDE's and run the server code, followed by the client (the server will wait for the client). 
